@namespace Showcase.Web.Client.Pages

@page "/fa-spinner/api"

<DemoPageHeader Title="Spinner: API" ComponentName="FaSpinner" DocSlug="fa-spinner" />

<ComponentSubNav BaseRoute="/fa-spinner" />

<div class="table-wrap">
    <table class="fa-table">
        <thead><tr><th>Parameter</th><th>Type</th><th>Notes</th></tr></thead>
        <tbody>
            <tr><td><code>Size</code></td><td><code>string</code></td><td>default <code>"24px"</code> — any CSS length, sets both width and height</td></tr>
            <tr><td><code>Variant</code></td><td><code>FaLoaderVariant</code></td><td><code>Accent</code> (default) · <code>Primary</code> · <code>Gold</code> · <code>Danger</code></td></tr>
            <tr><td><code>Label</code></td><td><code>string</code></td><td>default <code>"Loading"</code> — the <code>aria-label</code>, not visible text</td></tr>
            <tr><td><code>CssClass</code></td><td><code>string?</code></td><td></td></tr>
        </tbody>
    </table>
</div>
<p>No <code>AdditionalAttributes</code> splat on this component — every attribute you set is one of the above.</p>
