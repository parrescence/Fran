@namespace Showcase.Web.Client.Pages

@page "/theme-switcher/api"

<DemoPageHeader Title="Theme Switcher: API" ComponentName="FaThemeSwitcher" DocSlug="theme-switcher" />

<ComponentSubNav BaseRoute="/theme-switcher" />

<p>No parameters — <code>&lt;FaThemeSwitcher /&gt;</code> takes nothing. Already baked into <code>FaHeader</code>; also droppable standalone anywhere. Light/Dark/Colorblind-safe buttons, plain vanilla JS (no Blazor JS interop) reading/writing <code>localStorage</code> and the page's <code>data-theme</code> attribute — see <code>Blazor/CLAUDE.md</code>.</p>
