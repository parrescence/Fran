namespace Fran.Components;

/// <summary>Where on the viewport <see cref="FaModal"/>'s dialog sits within the backdrop.</summary>
public enum FaModalPosition
{
    /// <summary>Centered both axes — the default.</summary>
    Center,
    Top,
    Bottom,
    Left,
    Right
}
